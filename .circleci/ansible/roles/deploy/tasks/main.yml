
---

- name: Copy a "sudoers" file on the remote machine for editing
  copy:
    src: 'files/artifact.tar.gz'
    dest: '/home/ubuntu'

- name: Executing node
  shell: |
    cd /home/ubuntu
    tar -xzvf my.tar.gz .
    ls
    npm install
    pm2 stop default
    pm2 start npm -- start