
---

- name: Copy a "sudoers" file on the remote machine for editing
  copy:
    src: 'files/artifact.tar.gz'
    dest: '/home/ubuntu'


- name: Executing node
  shell: |
    cd /home/ubuntu
    npm i tar.gz
    tar -vxf artifact.tar.gz  -C .
    rm -f artifact.tar.gz
    npm install
    pm2 stop default
    pm2 start npm -- start
  register: cmd_dev_out
- debug: 
    msg: "{{ cmd_dev_out }}"